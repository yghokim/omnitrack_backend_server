import { SvgellipsisDirective } from './svgellipsis.directive';

describe('SvgellipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgellipsisDirective();
    expect(directive).toBeTruthy();
  });
});
