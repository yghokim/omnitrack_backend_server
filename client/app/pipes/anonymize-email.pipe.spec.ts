import { AnonymizeEmailPipe } from './anonymize-email.pipe';

describe('AnonymizeEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new AnonymizeEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
